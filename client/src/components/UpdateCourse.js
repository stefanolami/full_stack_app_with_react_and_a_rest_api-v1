import React, { Component } from 'react';

export default class UpdateCourse extends Component {
    state = {

    };

    render() {
        return (
            <h1>UpdateCourses</h1>
        )
    }
}